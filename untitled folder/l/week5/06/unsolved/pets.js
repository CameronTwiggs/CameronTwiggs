// Code your dog object
var dogs = {
  raining : true,
  noise: "Woof!",
  makeNoise: function(raining) {
    console.log(this.noise)
  }
};

// Code you cat object
var cats = {
  raining : true,
  noise : "Meow!",
  makeNoise: function() {
   console.log(this.noise)
  }
};

// Insert code here to make some noise >>> bark or meow!



// create a function called "massHysteria" which takes in both objects and
// prints a message to the screen if ".raining" is true for both of them
var massHysteria = function(dogs, cats) {
  
};

massHysteria(dogs, cats);